# Karpathy LLM Framework Vault - Python 의존성
# Python 3.9+ 권장

# ─────────────────────────────────────────────────────────
# 핵심 (필수)
# ─────────────────────────────────────────────────────────
duckdb>=1.0.0          # 그래프 캐시 + 벡터 cosine SQL
rank-bm25>=0.2.2       # Sparse 검색 (BM25)
mcp>=1.0.0             # MCP 서버 (Cursor/Claude Desktop 연동)

# ─────────────────────────────────────────────────────────
# 설정 파일 파싱 (선택; 없으면 내장 최소 블록-YAML 파서로 fallback)
# 00_System/Retrieval Policy.yaml을 더 견고하게 읽고 싶을 때만 설치한다.
# retriever가 try-import로 자동 감지하므로 미설치여도 동작한다.
# ─────────────────────────────────────────────────────────
# pyyaml>=6.0          # 선택

# ─────────────────────────────────────────────────────────
# 데몬 (필수) — 표준 구성. 단일 소유자 데몬이 DuckDB를 독점하고, 모든 MCP 클라이언트가
# localhost HTTP로 프록시 포워딩한다(다중 클라이언트 동시성 + Windows 호환). mcp_server.py는
# 데몬의 얇은 프록시이며 in-process DB 경로는 없다. See docs/DAEMON_DESIGN.md, SETUP.md '데몬' 절.
# ─────────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0

# ─────────────────────────────────────────────────────────
# Ollama는 Python 의존성이 아닌 별도 시스템 설치 필요
# https://ollama.com/download
# 설치 후: ollama pull bge-m3
# ─────────────────────────────────────────────────────────

# ─────────────────────────────────────────────────────────
# 의도적으로 배제된 의존성 (llm.c 철학: 의존성 최소화)
# ─────────────────────────────────────────────────────────
# - sentence-transformers / torch → 너무 무거움. Ollama로 대체
# - openai / anthropic → 외부 API 의존 회피. 로컬 우선
# - networkx → DuckDB SQL로 그래프 연산 가능 (필요 시 추가)
# - langchain → 추상화 레이어 최소화 (직접 구현이 더 명확)
