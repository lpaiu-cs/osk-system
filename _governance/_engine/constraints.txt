# 재현용 판 고정 — **선택이다.** requirements.txt가 계약(허용 범위)이고, 이
# 파일은 CI가 수트를 통과시킨 정확한 판이다(v3.22.2 CI 실측: Windows·Ubuntu ×
# Python 3.11·3.12). 새로 설치한 기기에서 결과가 CI와 다르면 이것으로 같은 판을
# 맞춰 원인을 가른다:
#
#   python -m pip install -r _governance/_engine/requirements.txt \
#       -c _governance/_engine/constraints.txt
#
# 설치되지 않는 줄(다른 플랫폼 전용 등)은 무시된다. mcp·pydantic 등을 올릴 때는
# 이 파일도 함께 올리고 CI로 확인한다. numpy는 CI가 Python 판별로 받은 판이
# 다르다(2.5는 3.11을 지원하지 않는다).
annotated-types==0.8.0
anyio==4.15.1
attrs==26.1.0
certifi==2026.7.22
cffi==2.1.1
click==8.5.0
cryptography==50.0.1
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
httpx-sse==0.4.3
idna==3.20
jsonschema==4.26.0
jsonschema-specifications==2025.9.1
mcp==1.30.0
numpy==2.4.6; python_version < "3.12"
numpy==2.5.3; python_version >= "3.12"
pycparser==3.0
pydantic==2.13.5
pydantic-core==2.46.5
pydantic-settings==2.15.0
pyjwt==2.15.0
python-dotenv==1.2.3
python-multipart==0.0.32
pywin32==312; sys_platform == "win32"
PyYAML==6.0.3
rank-bm25==0.2.2
referencing==0.37.0
rpds-py==2026.6.3
sse-starlette==3.4.11
starlette==1.7.0
typing-extensions==4.16.0
typing-inspection==0.4.4
tzdata==2026.4; sys_platform == "win32"
uvicorn==0.53.0
